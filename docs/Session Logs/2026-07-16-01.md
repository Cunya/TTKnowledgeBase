# Session log — 2026-07-16

## 00:10 - `u` documentation refresh

- Confirmed the current published corpus: 73 concepts, 107 videos, and 1,686 evidence moments.
- Confirmed the review queue: 1,059 total candidates — 955 accepted, 103 deferred, and 1 rejected.
- Refreshed the source-progress timestamp, pipeline snapshot date, backlog refresh marker, and progress-page date label.
- Added the chronological session-log lesson to `docs/lessons-learned.md`.
- No content or pipeline processing was performed during this documentation-only update. No commit was created.

## 00:15 - Project, data, and backlog review

- Reviewed the processor CLI, publish models, Astro routes/layout, production workflows, navigation taxonomy, current public corpus, quality report, and source-progress data.
- Recomputed current corpus signals: 403 repeated-excerpt groups cover 936 moments; six concepts have no outgoing relation; seven navigation leaves still have no concept content; three concepts remain single-source; verified visual demonstrations remain at zero.
- Found stale backlog baseline values for visual statuses and single-source coverage, plus a misleading graph-status sentence. These are being corrected in the consolidated backlog.
- Recorded additional implementation risks: UTF-8 mojibake remains in user-visible Astro/workflow strings, the project-page `SITE_URL` is not repository-qualified in deployment, and progress/source inventories remain partly hand-maintained.
- No scrape, extraction, or commit was performed.

## 00:20 - Title capitalization audit

- Scanned all published concept labels, evidence excerpts/reasons, TOC moment titles, and source video titles for all-caps wording.
- Concept labels and generated moment text contain no all-caps title fragments. Ten original YouTube titles contain emphasis capitals; these are source metadata, not missed transcript rephrasing.
- Added a backlog task to provide a calmer display title while retaining the exact original title for attribution and source linking.

## 00:25 - Controlled continuation batch selected

- No pending queue items or unprocessed configured transcripts remained; the only extracted-only video (`m44MwRy-sT8`) has deferred-only candidates.
- Selected eight unconfigured but catalogued GlobalTTStudio videos to address source diversity and empty shot leaves: forehand loop timing/backspin, backhand kinetic chain/flick, push, block, and banana flick.
- The batch is intentionally limited to eight videos and will use the normal paced ingest and block circuit breaker. No broad discovery scrape is being started.
- First pair result: `sosNHzR8A9U` ingested successfully; `saJV_mSKHaw` is members-only and was retained as inaccessible without retrying.
- Second pair result: `2Wk2xe0I1s8` ingested successfully; `MS5-0DaZ_h8` is also members-only and will be marked inaccessible without retrying.

## 00:46 - Controlled batch completed

- Six public GlobalTTStudio videos were ingested, extracted with the configured `gpt-5.4-mini` Codex profile, triaged, and published.
- The batch added 46 accepted evidence moments and left 11 candidates explicitly deferred. Two members-only videos were marked inaccessible and not retried.
- Published corpus now validates at 73 concepts, 113 videos, and 1,732 evidence moments; queue state is 1,001 accepted, 114 deferred, and 1 rejected.
- Updated the source-progress page/documentation, README snapshot, backlog baseline, quality report, and progress-page source counts. No commit was created.
- Verification passed: published-artifact validation, 26 pytest tests, Ruff, Astro diagnostics/build (195 static pages), and `git diff --check`.

## 00:47 - Next controlled batch selected

- The queue is fully triaged and every selected configured video has cached normalized and extracted artifacts; no local candidate work remains.
- Selected six additional GlobalTTStudio catalog videos for empty shot branches and correction coverage: counter-topspin, forehand flick, forehand loop errors, forehand block, block racket angle, and compact backhand/receive mechanics.
- This remains a small controlled batch; members-only detection, pacing, and the block circuit breaker remain active.
- All six videos ingested and extracted. The first triage attempt hit a stale `.yaml.tmp` replace permission error; removing that generated temp file and retrying completed triage with 36 accepted and 20 deferred candidates from this batch.

## 00:59 - Batch 14 published

- Published corpus now validates at 73 concepts, 119 videos, and 1,784 evidence moments; queue state is 1,037 accepted, 134 deferred, and 1 rejected.
- GlobalTTStudio now has 21 eligible/published videos, with 11 fully reviewed and 10 retaining deferred candidates. Four configured entries remain members-only.
- Refreshed the progress page/documentation, pipeline snapshot, README, quality report, and backlog metrics. No commit was created.
- Verification passed: published-artifact validation, 26 pytest tests, Ruff, Astro diagnostics/build (201 static pages), and `git diff --check`.

## 01:03 - Transcript-framing title audit

- Found that visible moment titles can inherit extraction wording such as “The transcript explicitly defines ...”.
- Will add deterministic display normalization for transcript/provenance leads while preserving the canonical source-backed excerpt and reason.

## 01:06 - Transcript-framing title fix complete

- Updated the shared evidence display helper so transcript and speaker reporting leads are removed from visible moment titles; definition phrasing is rewritten as a standalone label plus explanation.
- The canonical YAML and published corpus remain source-grounded and unchanged; only the rendered title/reason presentation is normalized.
- Astro diagnostics and static build passed (201 pages). The generated relax/accelerate concept page contains the standalone title and no longer contains the old transcript-framed title.

## 01:08 - Controlled processing continuation

- Shortcut cp requested. Checking cached pending candidates before selecting any new source batch.
- No large scrape will start while the queue or block safeguards require review.

## 01:14 - Controlled batch 15 published

- The cached queue was empty before acquisition, so six unconfigured GlobalTTStudio catalog entries were selected for push progression, defensive rhythm, contact drills, and forehand flick coverage.
- Two public videos (`7jWqEsHtN3Y`, `hwT7cH6PbZQ`) ingested and extracted sequentially with the configured `gpt-5.4-mini` low-reasoning profile. Four entries were confirmed members-only and marked inaccessible without retrying.
- Triage accepted 23 candidates, added 30 evidence moments, and deferred 5. A stale generated `counter-loop.yaml.tmp` caused one retry; only that generated temp file was removed before retrying.
- Published corpus now validates at 73 concepts, 121 videos, and 1,814 evidence moments. The queue contains 1,060 accepted, 139 deferred, and 1 rejected item, with no pending candidates. No commit was created.
- Verification passed: published-artifact validation, 26 pytest tests, Ruff, Astro diagnostics/static build (203 pages), and `git diff --check`.
- Final documentation refresh verified with the local build again at 203 pages; queue totals are 1,060 accepted, 139 deferred, and 1 rejected with no pending items.

## 01:20 - Moment boundary analysis study

- User requested a study of how to avoid evidence moments starting or ending mid-sentence and how to treat very short (for example, three-second) moments that lack enough instructional meaning.
- Reviewed the current caption-segment span selection, optional spoken-context boundary, authorized local ASR path, and separate visual-window model. No processing behavior was changed in this study pass.

## 09:46 - Boundary study automation split

- User requested an explicit split between work that can be implemented with deterministic scripts and work that benefits from LLM assistance.
- Will add an automation matrix and guardrails to the existing moment-boundary study; no pipeline behavior changes are planned.

## 09:47 - Boundary study automation split complete

- Added a phase-by-phase matrix distinguishing script-only work, optional LLM judgment, and human approval. The study now defines a structured LLM contract that forbids invented timestamps and keeps deterministic validation in charge.

## 09:49 - Phase labels made explicit

- User asked for the script-versus-LLM distinction to appear directly in the staged implementation phases, not only in the later matrix.
- Will label each phase with its automation level and permitted LLM role in the study.

## 09:50 - Phase automation labels complete

- Revised the staged plan to show five phases with explicit automation levels: script-only caption baseline, script-plus-review context handling, signal-processing alignment, targeted LLM semantic proposals, and script-only validation/publication.

## 09:53 - Daily LLM budget implementation

- User requested an adjustable per-day LLM budget covering different task types, with extraction/rephrase/benchmark work deferred when the configured allowance is exhausted.
- Reviewed the Codex invocation, token-usage provenance, CLI commands, benchmark path, and private manifest layout. Will implement a local token ledger, task caps, status command, and graceful deferral before changing extraction behavior.

## 10:04 - Daily LLM budget complete

- Added `processors/llm_budget.py` with configurable daily and per-task token caps, conservative pre-call reservations, usage reconciliation, private per-KB ledgers, and deferral records.
- Wired extraction, excerpt rephrasing, publishing's optional auto-rephrase, and model benchmarks to the shared budget. Added `llm-budget --kb <id>` status output and ignored local ledger/deferred files.
- Updated config, README, pipeline/operations docs, backlog P1-15, lessons learned, and refactoring notes. No commit was created.
- Verification passed: 30 pytest tests, Ruff, processor validation, publish, Astro diagnostics, and a 203-page static build.

## 10:05 - Local progress budget status

- User requested the LLM budget status on the progress page.
- Will read the private per-KB ledger and local processor config during the local-only Astro page build, show daily/per-task usage and deferrals, and keep the section absent from production builds.

## 10:09 - Local progress budget status complete

- Added a local-only LLM budget section to `app/src/pages/progress.astro`. It reads the configured daily/task limits and the private per-KB ledger, then shows daily usage, reserved tokens, remaining allowance, timezone/date, calls, and deferrals by task.
- Added responsive budget summary/table styles and Node file-module declarations for the Astro type checker. The section is gated by `PUBLIC_PRODUCTION_BUILD` and the existing production build script removes the progress page before public output.
- Verification passed: local Astro check/static build (203 pages), production-mode build (200 pages with no `/progress/`), and `git diff --check`. No commit was created.

## 10:12 - GlobalTTStudio eligibility audit

- User reported that the GlobalTTStudio eligible count looked wrong.
- The source configuration contains 31 GlobalTTStudio entries: 23 selected/public and 8 explicitly marked members-only. The corpus contains 23 GlobalTTStudio videos, so the eligible count of 23 is numerically correct.
- The confusing part was the progress bar label: “Selected from discovery” used the configured total (31), which includes the 8 ineligible members-only entries. The page now labels that stage “Configured from discovery,” derives the configured/eligible totals from the corpus and inaccessible set, and displays the public/members-only split directly.

## 10:15 - GlobalTTStudio eligibility audit complete

- Updated the local progress page to show `23 eligible · 8 members-only` for GlobalTTStudio and to use “Configured from discovery” for the 31-entry configured stage.
- Derived GlobalTTStudio and total configured/eligible counts from the current corpus and inaccessible-ID set instead of repeating the values in multiple table/bar locations.
- Verification passed: Astro check/static build (203 pages) and `git diff --check`. No commit was created.

## 10:15 - Documentation refresh

- Ran the `u` documentation shortcut after the eligibility audit.
- Confirmed the session log, session notes, backlog, and source-progress report reflect the current 31 configured / 23 eligible / 8 members-only GlobalTTStudio state and the local-only LLM budget status.
- No code changes, processing runs, or commit were made during this documentation pass.

## 10:16 - README / MomentGraph identity

- User requested that the README use the MomentGraph name and cover the current project aspects.
- Will update the README to describe the source-grounded wiki model, multi-KB architecture, local Codex/LLM budget, public/private boundaries, GitHub Pages deployment, local-only operator pages, and future offline scope.

## 10:17 - README / MomentGraph identity complete

- Replaced the generic README title and overview with the MomentGraph product identity while noting the current `TTKnowledgeBase` repository name.
- Documented the concept/wiki model, architecture, repository map, multi-KB setup, controlled pipeline, Codex model and budget, local-only operator pages, GitHub Pages deployment, privacy/source safeguards, future offline boundary, and release checks.
- `git diff --check` passed. No code changes, processing runs, or commit were made.

## 10:18 - Progress summary percentages

- User requested parenthesized percentage values beside the numeric stage totals in the progress summary table.
- Will use the existing progress denominators: configured/discovered, eligible/configured, and ingested/extracted/reviewed/published against eligible. Non-stage metrics such as accepted candidates and evidence moments will remain counts because they do not have a meaningful completion denominator in this table.

## 10:19 - Progress summary percentages complete

- Added parenthesized completion percentages beside configured, eligible, ingested, extracted, reviewed, and published counts in the local progress summary table.
- Added a note explaining each denominator and verified representative output such as `99 (33%)`, `23 (74%)`, and `121 (99%)` in the generated page.
- Astro check/static build passed with 203 pages and `git diff --check` passed. No commit was created.

## 10:57 - Daily timer automation review started

- User asked for a review of whether the processing pipeline can run daily from a timer without ChatGPT app assistance, while retaining Codex CLI as the optional LLM subprocess.
- Reviewed the current pipeline and operations documentation before making changes. The review will distinguish deterministic Python work, Codex CLI work, and editorial gates, then document timer-safe gaps, idempotency risks, budget/block handling, scheduler boundaries, and a proposed daily-run contract.

## 11:01 - Daily timer automation review complete

- Added `docs/daily-pipeline-automation-review-2026-07-16.md` with the ChatGPT-app/Codex-CLI distinction, current stage inventory, P0/P1/P2 findings, a timer-safe state machine, Windows Task Scheduler boundary, exit classes, implementation sequence, and acceptance criteria.
- Added P1-16 to the consolidated backlog for a dry-run-first `daily-run` orchestrator, lock, idempotent extraction workset, private run manifest, and scheduler wrapper. The item is planned; no timer was registered and no processing behavior changed.
- Added the orchestration and extraction-idempotency opportunities to `docs/refactorings.md`, updated session notes, and verified `git diff --check`. No commit was created.

## 11:08 - README table-tennis KB section complete

- Added a dedicated Table Tennis knowledge base section to `README.md` covering source channels and availability, current learning scope, depth-aware topic hierarchy, spoken versus visual evidence, per-video chronological TOCs, and editorial/publication boundaries.
- Kept the section domain-specific while preserving the generic multi-KB architecture and documented the provisional coach attribution and members-only handling accurately.
- No processing or build behavior changed. `git diff --check` passed after the documentation edit; no commit was created.

## 11:07 - Backlog reconciliation with recent studies

- Reconciled the consolidated backlog against the daily timer automation review, moment-boundary study, TOC automation review, quotation audit, legal/precedent reviews, and the 2026-07-16 project review.
- Added a study-to-backlog reconciliation table, refreshed the backlog timestamp, expanded P2-06 with the deterministic TOC fallback and validation contract, and added P1-17 for excerpt classification and overlap-regression protection.
- Updated the recommended execution sequence to include moment boundaries, timer orchestration, and quotation-risk regression work. No processing behavior changed and no commit was created.

## 11:14 - Local backlog page complete

- Added `app/src/pages/backlog.astro`, a digestible priority-lane and expandable-item view generated directly from `docs/prioritized-backlog-2026-07-15.md`.
- Added a local-only Backlog navigation link, responsive styles, and production-build exclusion alongside Pipeline, Progress, and Recent. Updated the README local operator-page list.
- Verification passed: Astro check (0 errors/warnings/hints), local Astro build (204 pages including `/backlog/`), production-flagged build (200 pages with `/backlog/` absent), source restoration, and `git diff --check`. No commit was created.

## 11:16 - Local backlog 404 resolved

- The local server on port 4321 was Astro preview serving the previous production-flagged `dist/`, where local-only pages are intentionally absent. Rebuilt the local Astro artifact with `npm run build:astro` so preview includes `/backlog/`.
- Confirmed `GET http://127.0.0.1:4321/backlog/` returns 200 and contains the backlog heading and P1-16. The production exclusion remains unchanged.

## 11:30 - Management navigation consolidated

- Replaced the separate local Pipeline, Progress, Backlog, and Recent header links with one local-only Dashboard link.
- Added `app/src/pages/dashboard.astro` as a management hub linking to all four focused views, with current corpus/backlog signals and a clear private/public boundary.
- Added dashboard styling, production exclusion, and README documentation. Verification passed: Astro check, local 205-page build with `/dashboard/`, production-flagged 200-page build without dashboard/operator pages, local artifact rebuild, HTTP 200 for `/dashboard/`, and `git diff --check`. No commit was created.

## 11:35 - Backlog reorganized around feature outcomes

- Replaced the priority-first backlog hierarchy with four feature workstreams: trust and source safety, evidence and content quality, publishing and navigation, and operations and automation.
- Kept each P0/P1/P2 label and status visible on the individual work item so priority remains actionable without dominating the page structure.
- Tightened feature-section spacing and retained responsive behavior. Verification passed: Astro check, local build with `/backlog/`, HTTP 200 route/content check, and `git diff --check`. No commit was created.

## 11:36 - Study-to-backlog synchronization rule documented

- Added a repository rule requiring every study to update the canonical Markdown backlog, rebuild and verify the generated local `/backlog/` HTML view, and update relevant pipeline/operations/README or findings documentation.
- Documented the contract in `docs/pipeline.md`, `docs/operations.md`, and `README.md`, including the distinction between canonical Markdown and generated local HTML. No processing or content changes were made; no commit was created.

## 11:37 - Table-tennis KB README link added

- Added direct links to the published and local table-tennis knowledge-base landing pages in the README. No site or processing behavior changed; no commit was created.

## 11:56 - Backlog hierarchy gained a feature layer

- Reframed the four existing workstreams as feature areas and added a middle feature layer beneath each area before the individual P0/P1/P2 backlog items.
- Added area-level progress, feature-level descriptions and completion counts, nested indentation, and responsive styling while preserving deep links to areas, features, and item IDs.
- Verification passed: Astro check, local 205-page build, HTTP 200 backlog route with area/feature/item content, and `git diff --check`. No commit was created.

## 16:12 - Cached processing checkpoint (`cp`)

- Ran `process-pending --kb table-tennis`; the cached queue produced 0 accepted, 0 deferred, 0 rejected, and 0 new evidence moments.
- Did not start another discovery or ingestion batch because the backlog still has open P0 expansion gates for acquisition basis, source quarantine/removal, permissions/removal communication, and the public/private boundary.

## 16:22 - Controlled batch 16 processed

- Selected two public TT SpinMaster videos from the existing discovery catalog, recorded them in `sources.yaml`, and ingested them with a 25-second request delay plus jitter: `D1cBF0nsjKo` and `Kg4hLYnEbAs`.
- Ran the configured Codex extraction, review-queue rebuild, and cached triage. The batch produced 12 accepted matches/evidence moments and 6 explicitly deferred candidates.
- Updated the source-progress snapshot and P1-02 backlog evidence to 128 published videos, 1,826 evidence moments, 1,072 accepted candidates, and 145 deferred candidates. Validation/publish, 30 tests, Ruff, Astro check/build, route checks, and `git diff --check` passed.

## 16:28 - Progress page synchronized with batch 16

- Updated the local progress page’s summary, completion stages, backlog counts, latest-batch notice, and TT SpinMaster inventory to match the new 101 configured/100 published source state.
- Verified the local `/progress/` route exposes the 1,826 evidence count and `D1cBF0nsjKo`; the 207-page local build and `git diff --check` passed.

## 16:35 - P1-14 caption boundary baseline implemented

- Added `processors/boundaries.py` with deterministic caption-unit merging, contiguous-cluster assessment, sentence-boundary flags, snapped-bound proposals, and a private corpus report; no LLM or audio processing is used.
- Updated `process-pending` to defer newly flagged candidates with explicit P1-14 review notes. Existing reviewed YAML remains unchanged because proposed snapped bounds are not claim support until reviewed.
- Added `report-boundaries` CLI coverage and boundary fixtures. The non-demo baseline found 1,826/1,826 evaluable moments: 16.4% starts mid-sentence, 17.5% ends mid-sentence, 3.5% are under four seconds, and 31.9% need context review. Wrote `docs/moment-boundary-report-table-tennis-2026-07-16.md` and reconciled the study/backlog/pipeline/operations docs.
- Focused tests and Ruff passed. Full validation, publish, pytest, and Astro build remain to be run before this change is complete; no commit was created.

## 16:38 - P1-14 verification complete

- Refreshed the sanitized corpus and confirmed published-artifact validation: 73 concepts and 123 videos.
- Full verification passed: 34 pytest tests, Ruff, `git diff --check`, Astro diagnostics, and the 207-page local build. Local `/backlog/` and `/progress/` both returned HTTP 200; `/backlog/` includes P1-14.
- Added the private boundary-report manifest to `.gitignore`; the Markdown baseline remains the reviewable documentation artifact. No commit or staging was performed.
- Re-ran the Astro diagnostics/static build after the final publish refresh; 207 pages built with 0 diagnostics.

## 16:42 - Backlog status ordering and color coding

- Updated the local-only backlog view to sort non-complete items before completed history within every feature, while preserving the existing hierarchy and item order otherwise.
- Added status-tinted row backgrounds and a legend for Open, Planned, Blocked, and Complete. Corrected status parsing so planned text mentioning “complete” is not misclassified as complete.
- Verification passed: Astro diagnostics, 207-page build, local `/backlog/` HTTP 200/order check, Ruff, and `git diff --check`. No commit or staging was performed.

## 16:55 - Deterministic project progress counts

- Added `processors/progress.py`, which derives source and project counts from discovery manifests, configured videos, normalized transcripts, derived candidates, review decisions, and the sanitized corpus without invoking an LLM.
- `publish` now writes `app/src/data/generated/<kb>-progress.json`; the local progress page reads this snapshot for workflow, evidence, visual, backlog, members-only, and inventory counts.
- Updated the pipeline README/backlog documentation to make the generated snapshot the source of truth. The quality-report generator remains a follow-up.
- Refreshed publish output and passed Astro diagnostics/static build. No commit or staging was performed.

## 16:58 - Documentation update

- Recorded the deterministic progress-snapshot change in the session documentation and notes.
- Confirmed the live progress route, generated count artifact, validation, tests, Ruff, Astro build, and diff hygiene remain clean.
- No commit or staging was performed.

## 17:05 - P1-14 gold-set review tooling

- Added deterministic stratified boundary-review export and provenance validation. The worksheet samples 24 flagged moments from the 582-moment flagged pool and keeps all review actions pending rather than inventing labels.
- Added `prepare-boundary-review` and `validate-boundary-review` CLI commands, boundary review tests, and documentation for the manual review contract.
- Updated the P1-14 study and backlog status: the remaining work is genuine reviewer decisions and a second measured report; no reduction is claimed yet.

- Final verification: published artifacts valid, 35 pytest tests passed, Ruff passed, Astro diagnostics/static build passed with 207 pages, and `git diff --check` passed. No commit or staging was performed.

## 17:12 - Dashboard navigation layout fix

- Fixed the local dashboard management index inheriting the global flex navigation rule. `.dashboard-index` now establishes a full-width grid, so Progress, Backlog, Pipeline, and Recent additions render as four stacked rows instead of shrinking into overlapping columns.
- Verified the local `/dashboard/` route returns HTTP 200. The rendered links occupy distinct full-width rows at the desktop viewport, and the Astro diagnostics/static build completed with 0 diagnostics and 207 pages.
- No commit or staging was performed.

## 17:17 - Per-video concept summaries

- Added a chronological Concept summaries layer to each per-video contents page. It uses the reviewed `short_definition` and optional `detailed_definition` for every concept, links the concept title to its article, and links the first-appearance time directly to the corresponding evidence row.
- Kept the detailed moment list linear and unchanged below the summaries; compact video-list TOCs remain compact to avoid making the index page larger.
- Verified a representative video route renders 6 concept summaries and 14 linked moments. Astro diagnostics/static build passed with 0 diagnostics and 207 pages; no commit or staging was performed.

## 20:49 - Concept summary smoke test

- Tested the `counter-loop` concept article: its reviewed definition renders independently from the three timestamped evidence moments.
- Tested the multi-concept video route `/kb/table-tennis/videos/G7eiadOxN8E/`: 12 summaries and 34 chronological moments rendered. Every summary had non-empty text, a concept overview link, and a first-moment anchor; all checks passed.
- The route returned HTTP 200 and `git diff --check` passed. No code changes, commit, or staging were performed during this test.

## 21:06 - Explicit concept Summary section

- Added a labeled Summary section and Contents link to every concept article. The section presents the reviewed short definition, optional detailed definition, and a clear boundary between editorial synthesis and timestamped evidence.
- Verified `/kb/table-tennis/concepts/counter-loop/`: Summary heading and text are present, the Contents link targets `#summary`, and the three video moments remain separate. Astro diagnostics/static build passed with 0 diagnostics and 207 pages; the route returned HTTP 200.
- No commit or staging was performed.

## 21:10 - Moment-based concept essay summary

- Added an optional reviewed `evidence_summary` field to the Concept model, published corpus type, and concept article template.
- Populated Counter-loop with a source-grounded essay that synthesizes the three reviewed moments (patient topspin exchange, pause for rotation and power, physical endurance, and timing/rhythm), while keeping the core definition separate.
- Concepts without an authored synthesis now show their reviewed definition with an explicit note rather than implying that a moment essay exists.
- Fixed the YAML representation with a folded scalar after validation caught the unquoted prose. Validation and publish passed (73 concepts, 128 videos, 1,826 evidence items), pytest passed (35 tests), Ruff passed, Astro diagnostics/build passed (0 diagnostics, 207 pages), and the local Counter-loop route returned HTTP 200. Browser verification found the Summary heading, essay, core definition, Contents link, and three moments.
- No commit or staging was performed.

## 21:13 - Expand evidence summaries to the concept corpus

- The Counter-loop essay confirmed the desired format: a moment-grounded narrative separate from the general definition. An inventory found 73 concepts with approved evidence reasons, but only Counter-loop had an authored `evidence_summary`.
- Next change will add a deterministic, non-LLM summary builder that turns approved evidence reasons into a concise reviewable paragraph for concepts missing this field. It will preserve source grounding and avoid transcript quotation; the existing Counter-loop essay will remain untouched.

## 21:19 - Evidence essays for all concepts

- Added `processors.evidence_summaries` and the `build-evidence-summaries` CLI command. The command previews by default and writes only missing fields with `--write`; it combines approved evidence reasons, does not call an LLM, and never overwrites existing reviewed prose.
- Populated all 72 missing concept files, bringing the corpus to 73/73 concepts with `evidence_summary`; Counter-loop’s hand-authored essay was preserved.
- Updated the pipeline guide and backlog. Validation/publish passed (73 concepts, 128 videos, 1,826 evidence items), pytest passed (38 tests), Ruff passed, Astro diagnostics/build passed (0 diagnostics, 207 pages), and `/kb/table-tennis/concepts/active-block/` returned HTTP 200 with the generated essay block.
- No commit or staging was performed.

## 21:22 - Summary wording cleanup

- Checked the generated paragraphs for transcript-framing language and quotation marks. Reflowed the YAML blocks and removed those artifacts from the summaries without changing the underlying evidence records.
- Added a regression test requiring every reviewed concept to carry an `evidence_summary`; the focused summary tests now pass (4 tests).

## 21:41 - Incremental evidence-summary refresh

- Added `evidence_summary_source_hash` to generated concept essays and refresh logic that compares approved evidence IDs, statuses, and reasons.
- `publish` now refreshes only generated summaries whose approved evidence changed; the hand-authored Counter-loop essay remains untouched. The explicit CLI path is `build-evidence-summaries --kb table-tennis --write --refresh-generated`.
- Added regression coverage for missing-summary insertion, stale-summary detection, and refresh after a reason change. Validation passed for 73 concepts, 128 videos, and 1,826 evidence items; all 40 tests, Ruff, and the Astro build (207 pages, 0 diagnostics) passed.
- Updated pipeline/backlog documentation. No commit or staging was performed.

## 21:49 - Direct concept-synthesis wording

- Reworked the deterministic summary normalizer so generated essays state the combined technique ideas directly instead of saying “the source,” “the evidence,” “the transcript,” “the speaker,” or “the coach.”
- Versioned the generated-summary format and refreshed all 72 generated essays; the hand-authored Counter-loop essay remained unchanged.
- Added regression coverage for attribution-free generated summaries. Validation passed for 73 concepts, 128 videos, and 1,826 evidence items; all 41 tests, Ruff, and the Astro build (207 pages, 0 diagnostics) passed. No commit or staging was performed.

## 21:50 - Summary phrase repair

- Tightened the direct-synthesis normalizer for coordinated clauses such as “force and warns that…” so the push-technique summary now reads as a direct instruction rather than a report about the source.
- Refreshed the 72 generated essays again, then reran validation, publish, all 41 tests, Ruff, and the 207-page Astro build with 0 diagnostics. No commit or staging was performed.

## 22:14 - Coherent Codex concept essays

- The deterministic reason joiner was not coherent enough: it exposed fragments and repeated labels. Added a schema-constrained Codex summary engine using the configured low-cost model, approved reasons only, private audit files, and the daily `summary` token budget.
- Batched the rewrite to reduce per-call overhead and rewrote all 72 generated essays. The quality gate now requires a 2–4 sentence paragraph, rejects bullets, quotation marks, source-reporting terms, and unsupported metadata; the hand-authored Counter-loop essay remains unchanged.
- Added generator metadata, batch tests/safeguards, and pipeline/backlog documentation. The Codex batch completed with 19 summary calls and 296,102 actual tokens recorded; no commit or staging was performed.

## 22:20 - Concise moment-title cleanup

- The shared display normalizer removed transcript-reporting leads, but definition/explanation phrasing such as “Defines the forehand drive as simple because ...” and “Specifies that ...” still appeared as sentence-like moment names.
- Tightened the display-only normalization for those patterns so TOCs and evidence headings use concise technique titles while canonical excerpts, reasons, timestamps, and provenance remain unchanged.

## 22:23 - Duplicate citation cleanup

- The Forehand drive concept contained two approved evidence records with identical source video, window, excerpt, reason, and segment IDs, which rendered as duplicate citation controls.
- Removed the redundant canonical record while retaining the original evidence ID and all provenance for the unique moment.

## 22:27 - Complete moment-title lead cleanup

- A corpus scan found that many excerpts still began with bare reporting verbs such as “Defines,” “States,” “Explains,” and “Warns”; the previous normalizer only handled transcript/speaker-prefixed forms.
- Expanded the shared display transform to remove those bare editorial leads across TOCs and evidence headings, leaving the reviewed canonical excerpt and provenance unchanged.
- Included the same coverage after “The speaker” so prefixed forms such as “The speaker defines ...” are normalized as well.

## 22:30 - Surface P1-14 progress status

- The local progress page showed aggregate pipeline counts but not the in-progress moment-boundary work item.
- Added a compact P1-14 status block with the measured 1,826-moment baseline, 582 flagged moments, the 24-item review sample, and links to the backlog and boundary report.

## 22:34 - Mark P1-14 explicitly in progress

- The backlog parser treated P1-14 as generic Open because its status described implementation progress without using an explicit state label.
- Changed the backlog status to “In progress” and added a dedicated backlog state/color so the item is distinguishable from planned and untouched open work.

## 22:37 - Keep P1-14 status in backlog only

- Removed the extra P1-14 section that had been added to the local progress page; the status and metrics remain on the backlog as requested.

## 22:40 - Improve backlog progress-state visibility

- In-progress used a green accent too close to Complete, and the expanded backlog item did not repeat its detailed status text.
- Changed In progress to an amber treatment and added a visible Status field inside every expanded backlog item, exposing P1-14’s measurements and remaining review work.

## 23:56 - Complete first P1-14 boundary-review pass

- Reviewed all 24 deterministic boundary-review worksheet items without inventing timestamps or changing cited segment IDs.
- Recorded 9 safe `merge` decisions with optional spoken playback context, 4 meaningful short-window `keep` decisions, and 11 conservative `defer` decisions that need source/audio inspection.
- Added the missing context metadata to the approved concept YAML and kept unresolved cases out of automatic publication.

## 23:58 - Build check follow-up

- Python validation, publication, tests, and Ruff passed. Astro diagnostics exposed an existing generated-data cast mismatch in the management pages (`facets` can omit optional keys); the safe `unknown` cast keeps the generated corpus contract explicit and allows the build to continue.

## 00:14 - Controlled CP batch

- The cached queue was empty, so the next controlled public batch was selected from the discovered catalog. The conservative ingest cap allowed 6 of 8 requested videos to finish before the command timeout; 2 remain uncached and were not retried aggressively.
- Codex extraction completed for the 6 cached videos using the configured low-cost model. Queue triage accepted 26 candidates into evidence moments and deferred 27 weaker candidates.
- The queue now contains 1,098 accepted, 172 deferred, and 1 rejected item with no undecided entries. Validation and publication passed at 73 concepts, 136 videos, and 1,851 evidence moments.

## 00:19 - Make Recent additions reflect ingestion

- The local Recent page was ordering only by YouTube publication date, so newly ingested older lessons were absent from its 18-item view.
- Added an ingestion timestamp to the video model and publish path, with a normalized-file mtime fallback for legacy cached videos. The local page now orders both lessons and connected concepts by ingestion time while retaining publication dates as source context.

## 00:27 - Fix video contents navigation layout

- The global navigation flex rule was leaking into the detailed video TOC because it is also a `nav` element. This placed concept summaries and chronological moments side by side instead of in the intended vertical hierarchy.
- Scoped `.video-toc` to `display: block` so its existing summary and linear-list layout rules apply correctly.

## 00:31 - Add video poster fallback

- The detailed video page still showed a large empty black player during the initial YouTube iframe load. Added a thumbnail poster/link fallback and eager iframe loading so the hero remains visually useful while preserving embedded playback.

## 01:02 - Controlled CP batch 2

- The local candidate queue was empty, so the next 8 public discovered videos were ingested with the normal pacing and all cached successfully.
- Four extractions completed before the configured extraction-task budget deferred the remaining four; triage accepted 21 candidates into evidence moments and deferred 8 weaker candidates.
- The queue now contains 1,119 accepted, 180 deferred, and 1 rejected item with no undecided entries. Validation and publication passed at 73 concepts, 144 videos, and 1,872 evidence moments. The four budget-deferred videos remain cached for the next budget window.

## 01:18 - Investigate clipped spoken moment endings

## 10:22 - Controlled CP batch 3

- The cached candidate queue was empty, so eight public forehand-loop lessons were ingested from the discovered catalog using the normal pacing and block circuit breaker. All eight normalized transcripts were saved; no YouTube block occurred.
- The configured extraction-task limit was already exhausted for the day. Each extraction request was therefore deferred safely, with no expensive Codex call made. The 1,300-item review queue remains unchanged and has no undecided local candidates to triage.
- Publication now contains 73 concepts, 152 videos, and 1,872 approved evidence moments. The eight cached transcripts await the next extraction budget window.

- Reviewed the `loose-grip-backhand-friction` moment in `TnKP4aH3ft8` against the normalized captions. Its cited segment IDs end on a complete caption unit, so the deterministic boundary report does not flag it as mid-sentence.
- The displayed 7:00–7:22 playback window still ends at the caption boundary, where YouTube timing can trim the final spoken tail. Plan: add a small, bounded playback tail for spoken citations only, without changing cited segment IDs or claiming additional evidence.
