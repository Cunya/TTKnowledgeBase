# Session log — 2026-07-21

## 10:35 - Adjust ASR media retention and subtitle provenance plan

- Updated the experimental media-ASR plan so downloaded video is retained in the private ignored `media/<kb>/<video-id>/` workspace for the initial implementation.
- Deferred cleanup preview, deletion, retention, and expiry controls to a later phase; retained-media state must remain explicit and media must never enter public output.
- Defined explicit transcript origins: `local_asr`, `youtube_manual`, `youtube_generated`, and `supplied`, so locally generated subtitles cannot be confused with downloaded subtitles.
- Synchronized the backlog, pipeline, and operations documentation with the revised plan.

## 10:40 - Remove per-batch operator-action requirement from ASR plan

- Removed the requirement that every media acquisition batch require an explicit operator action.

## 10:45 - Narrow initial ASR test scope

## 10:50 - Retain extracted ASR audio initially

- Updated the media-ASR plan to retain extracted audio alongside downloaded video.
- Deferred audio deletion to the future cleanup feature set.

## 10:20 - Implement retained-media ASR smoke test

- Added the separate `ingest-media-asr` one-video command with dry-run metadata inspection and bounded duration/size options.
- Retained downloaded video and extracted mono WAV audio under ignored `media/<kb>/<video-id>/` storage.
- Added private ASR VTT/JSON output, a per-video manifest, local ASR provenance, and `VIDEO_ID:asr:NNNNN` segment IDs.
- Kept normal ingestion, cp, monitor, scheduler, and publishing paths separate.

## 10:25 - Verify retained-media ASR smoke test

- Focused ASR tests passed: 12 tests; Ruff passed for changed files.
- Full pytest passed 45 tests with the pre-existing generated-summary failure in `compact-explosive-contact-and-stop.yaml`.
- `validate` retained the four known review-queue citation mismatches 1236–1239; `publish` completed with 345 concepts.
- Full Ruff passed and Astro check/build completed with 0 diagnostics and 884 pages.

## 10:35 - Fix deterministic summary reporting language

- Found the publish pytest failure in `neutral-ball-receive-handling.yaml`: its generated summary retained “the speaker noting that”.
- Planned a sanitizer rule for reporting constructions of the form “with the speaker/coach noting, saying, stating, explaining, or describing that”, followed by regeneration of the affected summary.

## 10:40 - Apply summary sanitizer fix

- Added the reusable deterministic rewrite and corrected the affected generated summary without changing its evidence or source hash.

## 10:45 - Verify publish summary fix

- Corrected two existing deterministic summaries that still contained source-reporting language.
- Evidence-summary tests passed 6/6, full pytest passed 46/46, Ruff passed, and publish completed with 345 concepts.

## 11:05 - Continue one-video ASR smoke test

- Selected cached video `_biE-Dzy0aM` (`3 Ways to Add Power Without Missing`) as the single test input.
- Downloaded and retained its 17.5 MiB video and extracted audio; the first transcription attempt was blocked because `faster-whisper` was not installed.
- Installed `faster-whisper` 1.2.1 directly after the editable extras install hit the environment's pip certificate issue.
- The `tiny` model downloaded, but automatic CUDA selection failed because `cublas64_12.dll` is unavailable; the next retry will use CPU explicitly.

## 11:20 - Isolate ASR normalized output from caption cache

- The ASR run produced 129 segments successfully, but validation showed that writing ASR output to the normal normalized-video path overwrote the existing caption-backed transcript for `_biE-Dzy0aM`.
- Corrective action: store ASR normalized JSON under `data/normalized/<kb>/asr/` so ordinary reviewed evidence remains unchanged; restore the original caption-backed cache before publishing again.

## 11:35 - Complete one-video ASR smoke test

- Restored `_biE-Dzy0aM` through normal caption ingestion and confirmed its original `youtube_generated` segments remained at `_biE-Dzy0aM:00000`.
- Reran the ASR test with the CPU `tiny` model into the isolated ASR cache; it produced 129 `local_asr` segments beginning `_biE-Dzy0aM:asr:00000`.
- Confirmed retained video/audio, VTT/JSON, manifest provenance, and absence from `app/public/`.
- Validation retained only the four known review-queue mismatches 1236–1239; publish completed with 345 concepts.

- Changed the ASR rollout plan to begin with one video only as a smoke test.
- Kept the 10–20-video gold-set evaluation as a later expansion after download, ASR provenance, retention, and boundary checks pass.

## 12:05 - Clear publish validation failure

- Updated review-queue validation to ignore accepted entries for excluded `demo_fixture` videos, matching normal corpus filtering.
- `validate` passed with 345 concepts, 520 videos, and 4,014 evidence items.
- `validate-published` passed with 345 concepts and 497 published videos.
- Publish completed with 345 concepts; full pytest passed 46/46 and Ruff passed.

## 12:20 - Add local ASR test-results dashboard

- Planned a local-only `/asr-test/` management page linked from `/dashboard/`.
- The page will show sanitized test metrics and created-material inventory without embedding raw transcript text, absolute filesystem paths, or private media in the browser corpus.

## 12:35 - Verify local ASR results dashboard

- Added the local `/asr-test/` page and linked it from `/dashboard/` as management view 11.
- The page reports the one-video result, 129 segments, model/device, transcript namespace separation, private artifact inventory, verification status, and deferred gates without loading private artifact contents.
- Local Astro build passed with 0 diagnostics and 885 pages; production-mode build passed with 0 diagnostics and confirmed `/asr-test/` was excluded from the public artifact.

## 12:50 - Automate final publish verification gate

- Added an executable `scripts/verify-release.ps1` gate that checks the processor monitor is idle, then runs validate, publish, validate-published, tests, Ruff, and the Astro build in order.
- Updated `AGENTS.md` so `validate-published` and the monitor-idle precondition are mandatory final verification steps.

## 12:55 - Verify release gate preflight

- Confirmed `scripts/verify-release.ps1` refuses to run while the active processor monitor/cp process tree is present, returning exit code 1 with the active PIDs.
- The full gate should be rerun after the monitor is stopped and the review queue is stable.

## 13:10 - Fix release-gate summary regression

- The stopped monitor left the reviewed queue stable, and both reviewed and published corpus validation passed.
- The complete release gate then exposed a persisted deterministic summary containing the possessive phrase `the speaker's`.
- Bumped the deterministic summary format version and added possessive-reference cleanup plus a regression test so publication refreshes stale generated summaries automatically.

## 13:25 - Complete release gate after summary fix

- Stopped the orphaned monitor process after verifying its exact command line and confirmed no monitor/cp process remains.
- Full release verification passed: reviewed validation, publish, published-artifact validation, 47 tests, Ruff, and Astro diagnostics/static build.

## 13:45 - Fix published-only demo queue detection

- CI exposed that accepted `demoTT00001` queue items were still validated when private normalized metadata was unavailable.
- Centralized synthetic demo-ID detection with a tracked fixture ID and added a regression test for published-only validation.
- Local `validate-published` now passes with 371 concepts and 508 published videos.
