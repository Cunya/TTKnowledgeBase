# Session log — 2026-07-20

## 10:00 - Operational hardening

- Investigated the stale `cp.latest.json` failure where `process-pending` returned exit 1 without a useful reason.
- Confirmed the direct command succeeds now; the orchestrator had discarded child stdout/stderr and retained only a generic stage exit code.
- Updated `scripts/run-cp.py` to capture bounded stage output and record explicit failure metadata.
- Updated the monitor to expose outcome, reason, and consecutive failure count, resetting the counter after success.
- Updated P1-16 and operations documentation.
- Verification: focused tests and full required checks run after the implementation.

## 10:15 - Video index divisions

- Found the KB video index rendering all 432 videos in one page.
- Planned a channel directory at `/videos/` plus channel-specific batches of 20 videos with static numbered routes.
- Replaced the all-videos index with channel cards and added static `/videos/<channel>/<batch>/` routes, each rendering at most 20 videos.
- Added responsive channel and batch navigation styling.
- Astro check/build passed with 0 diagnostics and 680 generated pages.

## 10:35 - Keep relevance values local-only

- Changed concept pages so relevance percentages are exposed only in local development; production keeps the sorted order without publishing the internal score.

## 10:45 - Limit concept evidence display

- Capped concept-page evidence rendering at the top 10 ranked moments per concept while preserving the full reviewed evidence count in the article overview.

## 10:55 - Local overflow evidence section

- Added a local-development-only section for ranked moments after the top 10; production remains capped at the top 10.

## 11:05 - Refined evidence caps

- Raised the per-concept displayed evidence cap to 15 and limited each evidence subsection to its 5 highest-ranked related moments.
- Adjusted the final cap to 20 total moments: four subsections with up to 5 moments each.

## 10:25 - Concept moment relevance ranking

- Confirmed evidence already carries reviewed confidence, visual status, excerpt, and reason fields.
- Planned a deterministic relevance score from those signals, shown as a ranking aid and used to order moments on concept pages.
- Added the score utility and sorted concept-page evidence highest-first; displayed the percentage beside each evidence group.
- Astro check/build passed with 0 diagnostics and 680 generated pages.

## 11:20 - Verify concept moment counts

- Investigated the Forehand Loop page count mismatch: global slicing happened before subsection selection, so category sections could be underfilled.
- Changed rendering to select up to five ranked moments independently for each subsection, with the four-section total capped at 20.
- Verified the local route reports the number of moments actually rendered by populated sections; a concept may show fewer than 20 when a category has insufficient evidence.
- Astro check/build passed with 0 diagnostics and 680 generated pages.
## 2026-07-20 16:05

- Investigating processor-monitor readability: the current page exposes raw output but does not explain stages or show active process resource usage.
- Planned local-only monitor improvements: stage progress, plain-language explanations, and a Windows process snapshot.
- Requested follow-up: let the processor monitor use the full available screen width.
- Requested follow-up: add an understandable whole-cycle cp progress bar; the percentage will be an operational estimate because extraction workload varies.
- Atlas audit: moved Hop step from Backhand loop to Fundamentals > Footwork and spacing, and moved Preparation stroke and backswing for backhand serve from Serve receive to Serve > Serve mechanics. Publish and Astro build passed; validate still reports four existing review-queue citation mismatches.
- Processor monitor follow-up: remove the inherited max-width, put whole-cycle progress at the top, and compact the process resource table.
- Processor monitor quality pass: replace the layered narrow page with a single full-width operational dashboard, keep progress first, give current output the main reading area, and demote process resource data into a compact side panel.

## 21:23 - Reconcile discovered versus configured video counts

- The local progress inventory reports 822 discovered entries versus 148 configured entries, but the cp workset intentionally processes only selected configured videos until backlog-gated expansion is approved.
- Reconciled the current artifacts: 434 normalized videos have matching candidate files; only four discovered IDs remain unnormalized, and all four are in retry, unavailable, or cooldown states.
- Found an operational reporting bug in `scripts/run-cp.py`: the discovery-loop local variable overwrote the `cp.latest.json` manifest path, so the completed cp report could be written into the last discovery manifest and leave the monitor reading stale data.

## 21:40 - Stream live cp stage output

- The monitor showed no ingest detail while a 40-video batch was running because `scripts/run-cp.py` used `subprocess.run(..., capture_output=True)` and only published output after the entire stage completed.
- Planned a bounded line-by-line subprocess reader with unbuffered child Python output so the existing monitor pipe can display skips, saves, failures, and stage progress as they happen.
- Implemented the streaming reader and verified it with a two-line subprocess smoke test. The subsequent controlled catalog run completed with 10 successful videos and streamed individual ingest outcomes into the monitor output.
