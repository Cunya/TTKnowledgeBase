# Session log — 2026-07-17

## 10:28 - Make local Recent additions include newly ingested videos

## 10:35 - Make Recent refresh script-owned

- Recent snapshot generation should not depend on an assistant remembering a follow-up step after CP. Move the normalized-video snapshot writer into the processor layer and call it from both ingestion and publication, so deferred extraction still appears immediately.

- The Recent page read only the sanitized published corpus, which intentionally omits videos without approved evidence. The latest CP batch therefore appeared in normalized storage and progress counts but not in the local Recent list.
- Add a script-generated local snapshot of all normalized video metadata during `publish`, and let the local-only Recent page show unprocessed lessons with an explicit pending-review state. Production still removes the Recent page and does not expose this snapshot through a route.
